<h1> Listing Users </h1>

<table>
    <%= for user <- @users do %>
 <tr>                        <%# user: user === parameter of the other template #%>
    <td><%= render "user.html", user: user%></td>
        <td><%= link "View", to: Routes.user_path(@conn, :show, user.id) %></td>
    <% end %>
</table>
